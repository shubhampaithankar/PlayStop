// Luxon format token for the display-only local label on each grid cell.
// ZZZZ renders the zone's long name (e.g. "Eastern Standard Time") so the
// same instant reads unambiguously across a DST boundary.
export const LOCAL_LABEL_FORMAT = "yyyy-MM-dd HH:mm ZZZZ";
