// Deliberately empty. This package exists to hold pure logic shared by more
// than one app, starting with slot/availability math in milestone 2. There
// is nothing to put here yet: milestone 1 has no booking logic anywhere, see
// the "Do NOT" list in the root CLAUDE.md. This is a placeholder, not an
// oversight, see README.md.
export {};
